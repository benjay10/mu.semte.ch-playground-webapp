import { helper } from '@ember/component/helper';

export default helper(function ifnotempty(args) {
  //if (args.length == 3) {
  //  let [ pred, cons, alt ] = args;
  //} else if (args.length == 2) {
  //  let [ pred, cons ] = args;
  //}
  //return pred ? cons : alt;
});
